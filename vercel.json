{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @artist-booking/shared build && pnpm --filter @artist-booking/ui build && pnpm --filter @artist-booking/web build",
  "outputDirectory": "apps/web/.next",
  "git": {
    "silent": false
  },
  "regions": ["sfo1", "iad1"],
  "env": {
    "NODE_ENV": "production"
  }
}
