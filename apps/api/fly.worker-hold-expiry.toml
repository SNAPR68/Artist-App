app = "artist-booking-worker-hold-expiry"
primary_region = "bom"

[build]
  dockerfile = "../../infrastructure/docker/api.Dockerfile"

[env]
  NODE_ENV = "production"

[processes]
  worker = "node dist/workers/hold-expiry.worker.js"

[[vm]]
  size = "shared-cpu-1x"
  memory = "256mb"
